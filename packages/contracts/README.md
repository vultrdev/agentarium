# packages/contracts

This package will own shared contracts for:
- runtime events
- world state
- commands
- replay semantics

If this package gets sloppy, the rest of the system follows it into the swamp.
